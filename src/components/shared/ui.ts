import styled from "styled-components";

type AboutSectionProps = {
  image: string;
  open: boolean;
};

export const PageContainer = styled.div`
  margin: 0 auto;
  max-width: 700px;
  padding: 15px;
`;